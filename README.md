# Event-Ticket-Booking-System
building a scalable, concurrent event ticket booking system using Node.js. The system manages ticket inventory, handles concurrent bookings, maintains waiting lists, and ensures data integrity under high-load conditions.
